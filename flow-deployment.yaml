apiVersion: logging.banzaicloud.io/v1beta1
kind: Flow
metadata:
  name: es-flow
  namespace: logging
spec:
  match:
     - select: {}
     - exclude: 
        labels:
          app: coturn
  localOutputRefs:
    - es-output
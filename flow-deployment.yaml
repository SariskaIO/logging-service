apiVersion: logging.banzaicloud.io/v1beta1
kind: Flow
metadata:
  name: es-flow
  namespace: logging
spec:
  filters:
    - record_transformer:
        remove_keys: kubernetes
  match:
     - select: {}
  localOutputRefs:
    - es-output